run "php server.php" at command line

You may need to change the port for websocket in chat.js (currently set up for port forwarding in homestead)

open index.php in browser

enter name

start talking